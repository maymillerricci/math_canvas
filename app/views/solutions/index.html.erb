<h1>Solutions Gallery</h1>
<div class="row"><h2 class="max_width"><%= @problem.question %></h2>
<%= link_to 'Draw This Problem!', new_problem_solution_path(@problem), class: :button, id: "draw_this_problem", 'data-ajax' => false %></div>

<ul id="solutions_list">
	<% @solutions.each do |solution| %>
			
		<li>
			<span>Name: <%= solution.username %></span>
			<span>Answer: <%= solution.answer %></span>
			<span>Likes: <%= solution.likes %></span>
			<%= link_to image_tag('like.png'), problem_solution_likes_path(@problem, solution), method: :post, id: 'like', title: 'Like!' %>
		</li>
			<%= image_tag solution.picture, class: :solution %>
	<% end %>
</ul>
